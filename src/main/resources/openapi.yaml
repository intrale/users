openapi: 3.0.0
info:
  title: Users API
  version: '1.0'
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
